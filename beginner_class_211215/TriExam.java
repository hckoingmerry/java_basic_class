public class TriExam {
    public static void main(String[] args) {
        
        /*
        삼항연산자(조건)
        - 만족하면 true, 아니면 false
        - 첫번째 피연산자 조건식의 평가 결과에 따라 true면 두번째 항, false면 세번째 항의 값을 반환한다. (조건식의 괄호는 필수 아님)
        - 여러번 중첩하면 가독성이 떨어짐

        ex) (조건식) ? true : false

        if (조건식) {
            true;
        } else {
            false;
        }

        Quiz. 삼항 연산자를 사용하여
                100점 미만 90점 이상이면 "A"
                90점 미만 80점 이상이면 "B"
                80점 미만 70점 이상이면 "C"
                나머지는 전부 "D"
        System.out.println(a >= 90 ? "A" : a >= 80 ? "B" : a >= 70 ? "C" : "D" )        
        */
    }
}
