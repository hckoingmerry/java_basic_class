public class FLogical {
    public static void main(String[] args) {
        
        /*
        논리연산자는 둘 이상의 조건을 and 또는 or로 연결하여 하나의 식으로 표현한다.

        ||(or): 피연산자의 어느 한쪽만 true면 최종값은 true로 반환
        && (and): 피연산자 양쪽 모두 동시에 true여야만 최종값을 true로 반환
        (10 < x < 20 이런 식으로는 표현하지 못함. 중간에 반드시 &&를 넣어야 함)

        Quiz2. 문자를 하나 입력 받아서 그 문자가 숫자인지 영문자인지를 출력하시오.
        Scanner sc = new Scanner(System.in);
        System.out.print("문자를 하나 입력하세요.> ");
        String a = sc.nextLine();
        char ch = ' ';
        ch = a.charAt(0);
        
        if (ch >= '0' && ch <= '9') {
        System.out.println("입력하신 문자는 숫자입니다.");
        } else if (ch >= 'a' && ch <= 'z' || ch >= 'A' && ch <= 'Z') { 
        System.out.println("입력하신 문자는 영문자입다.");
        }

        */
    }
}
